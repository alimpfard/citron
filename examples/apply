File new: 'extensions/fileutils.ctr', include.

var test is Nil.
{ test is Queue new. } catch: {:e Pen red write: e, brk. }, run.

import import: (Array < 'Queue').

test is Queue new.

Pen resetColor write: test, brk.
