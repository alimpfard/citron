#Can we use the unicode return arrow instead of the ascii one?

Number on: 'describe' do: {
	var text is Nil.
	( me = 0 ) ifTrue: {
		text is 'zero'.
	}.
	( me = 1 ) ifTrue: {
		text is 'one'.
	}.
	↑ text.
}.

Pen write: 0 describe + 1 describe, brk.
